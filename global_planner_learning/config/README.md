# 配置文件目录

此目录用于存放全局路径规划相关的配置文件。

## 计划创建的配置文件

- `global_costmap_params.yaml` — 全局代价地图参数
- `planner_params.yaml` — 全局规划器参数
- `move_base_params.yaml` — move_base 行为参数

参考现有配置：`/motion_ws/src/user_config/`
